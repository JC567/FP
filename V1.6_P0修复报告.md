# V1.6 P0 修复报告

## 一、修复概览

V1.6 P0 修复的核心目标：**让历史 PE、EPS_TTM、分红率以及回测真正做到 Point-in-Time（PIT），并消除重复 PIT 逻辑、版本选择错误和数据顺序依赖**。

本次修复遵循严格原则：不重构整个项目，仅针对红队审计发现的 P0 级问题进行精准修复。

## 二、修改文件

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `valresearch/data/pit.py` | 核心修复 | eps_ttm_asof 增加修订去重；annual_versions_pit/net_profit_ttm_asof 的 groupby().last() 改为 idxmax() 整行选择；PitLayer.asof 改用 pit_pe 自算 PE；pe_asof 标记为 legacy；新增可追溯性字段 |
| `valresearch/models.py` | 扩展 | PitSnapshot 新增 pe_valid/pe_source/report_period/announcement_date/revision_version 字段 |
| `valresearch/valuation/engine.py` | 修复 | _ttm_values/_ttm_step 排序改为 ['ann_ts','per_ts'] 稳定排序 |
| `tests/test_p0_pit_attacks.py` | 新增 | 11 项 PIT 攻击测试 |
| `scripts/smoke_test.py` | 新增 | 干净环境 smoke test |
| `requirements.txt` | 新增 | 项目运行依赖声明 |
| `README.md` | 更新 | V1.6 P0 说明、数据源局限性声明、测试表更新 |

## 三、PIT 架构变化

### 修复前

```
                Data Provider
                      |
               Raw Financial
                      |
         +-----------+-----------+
         |           |           |
  pit.py eps_ttm  engine.py     pit_pe.py
  (手工选择)     _ttm_values    (单点计算)
         |       (独立实现)        |
         |           |           |
         +-----------+-----------+
                     |
              PitLayer.asof()
              (使用外部 pe_asof)
```

### 修复后

```
                Data Provider
                      |
               Raw Financial
                      |
                PIT Version Layer
                (idxmax 整行选择)
                      |
                 PIT Snapshot
                      |
          +-----------+-----------+
          |           |           |
        PE/Pct      Payout      Quality
          |           |           |
          +-----------+-----------+
                      |
                   Gordon
                      |
                 Value Trap
                      |
                   Signal
                      |
                 Position
                      |
                   Backtest
```

**关键变化**：
1. PIT TTM 只有唯一实现（`pit.py` 中的 `eps_ttm_asof` / `net_profit_ttm_asof`）
2. `valuation/engine.py` 不再自行计算 PIT TTM，改为调用 `pit.select_financial_version()`
3. `PitLayer.asof()` 不再使用外部 PE，改为调用 `compute_pe_ttm_pit()`
4. 所有版本选择使用 `idxmax()` 整行选择，禁止 `groupby().last()` 跨列拼接

## 四、PIT TTM 算法

### eps_ttm_asof() 修复后算法

```
1. 过滤：announcement_date <= asof_date
2. 修订去重：groupby('report_period')['ann_ts'].idxmax() -> 整行选择
3. 排序：sort_values('per_ts')
4. 锚点：iloc[-1] = 最新已公告报告期
5. TTM 外推：
   - 年报(12月)：直接返回 EPS
   - 季报(Q1/Q2/Q3)：current + prev_annual - prev_same_quarter
   - 上年同期/上年年报也经过修订去重
```

### net_profit_ttm_asof() 算法

与 `eps_ttm_asof()` 完全对称，作用于 `net_profit_attr` 字段。

## 五、同公告日问题修复说明

**问题**：当 2024-12-31 年报和 2025-03-31 一季报的 `announcement_date` 同为 2025-04-30 时，`sort_values('per_ts')+iloc[-1]` 选择锚点的结果取决于 DataFrame 行顺序。

**修复**：在 `iloc[-1]` 之前增加 `groupby('report_period')['ann_ts'].idxmax()` 修订去重，确保同一报告期只保留最新版本，然后 `sort_values('per_ts')` 后 `iloc[-1]` 选择最新报告期。

**验证**：`test_same_announcement_date_multiple_periods` - 构造同公告日场景，随机打乱 DataFrame 10 次，结果完全一致。

## 六、多版本问题修复说明

**问题**：`groupby('report_period', as_index=False).last()` 对每列独立取最后一个非空值，可能产生跨版本字段拼接（EPS 来自版本 A，净利来自版本 B）。

**修复**：改为 `df.loc[df.groupby('report_period')['ann_ts'].idxmax()]`，保证整行来自同一 revision。

**影响范围**：
- `annual_versions_pit()` - 年度财报 PIT 视图
- `net_profit_ttm_asof()` - 归母净利润 TTM
- `eps_ttm_asof()` - 新增相同逻辑（之前缺失）

**验证**：
- `test_missing_field_revision` - 版本2 的 NetProfit 为 NaN，选中版本2 时 NetProfit 必须为 NaN
- `test_same_period_multiple_revisions` - asof 在 v1/v2 公告日前后选择正确版本

## 七、外部 PE 清理说明

**修复前**：`PitLayer.asof()` 调用 `pe_asof()` 使用外部 Baidu PE 数据。

**修复后**：
- `PitLayer.asof()` 改为调用 `compute_pe_ttm_pit(price, eps_ttm)` 自算 PE
- `pe_asof()` 标记为 `[LEGACY]`，不再被核心流程调用
- `pe_step()` 在 `engine.py` 中已标记 `[DEPRECATED P0-1]`
- 外部 Baidu PE 仅保留用于向后兼容和诊断对比

**验证**：`test_external_pe_not_used` - 外部 PE=100，PIT price=10，PIT EPS=1 -> PitLayer 返回 PE=10（非 100）

## 八、PIT Snapshot 说明

`PitSnapshot` 数据类新增可追溯性字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `pe_valid` | bool | PE 是否有效（price>0 且 eps>0） |
| `pe_source` | str | PE 来源：`PIT_CALCULATED` 或 `EXTERNAL_HISTORICAL_PE` |
| `report_period` | str | 锚点报告期（如 `2024-12-31`） |
| `announcement_date` | str | 锚点公告日（如 `2025-04-30`） |
| `revision_version` | int | 锚点修订版本号（1=首次，2=第一次修订...） |

**验证**：`test_pit_snapshot_traceability` - PitLayer.asof() 后 snapshot 包含所有追溯字段

## 九、数据源局限

**PIT Engine 能力**：
- 完整的修订版本选择（多版本时取 asof 时已公开的最新版本）
- 同公告日多报告期的确定性选择
- 随机行顺序不变性
- 未来数据完全隔离

**生产数据源限制**：
- 当前数据源（同花顺 THS）的 `announcement_date` 全部为 `ESTIMATED`（法规截止日近似）
- 真实公告日期可能与法规截止日有 1-30 天偏差
- 同一报告期的真实修订历史可能不完整
- 当真实数据源无法提供完整历史财务修订版本时，系统输出 `DATA_LIMITATION`

## 十、测试结果

### 现有测试（24个文件）

全部通过

### V1.6 P0 新增攻击测试（11项）

```
test_same_announcement_date_multiple_periods PASS
test_pit_is_order_invariant PASS
test_same_period_multiple_revisions PASS
test_missing_field_revision PASS
test_future_revision_excluded PASS
test_future_period_excluded PASS
test_same_day_multi_report_revision PASS (100 shuffles x 3 dates)
test_pit_ttm_correctness PASS
test_pe_is_price_divided_by_pit_eps PASS
test_external_pe_not_used PASS
test_pit_snapshot_traceability PASS

=== Summary: 11 passed, 0 failed, 11 total ===
```

### compileall

valresearch/ 全部通过

### Smoke test

ALL STEPS PASSED

## 十一、测试覆盖的攻击场景

| 攻击场景 | 测试函数 | 验证内容 |
|----------|----------|----------|
| 同公告日多报告期 | `test_same_announcement_date_multiple_periods` | 2024FY 和 2025Q1 同日公告，asof 选择正确报告期 |
| 随机行顺序不变性 | `test_pit_is_order_invariant` | 5 个报告期打乱 5 次，结果完全一致 |
| 同报告期多版本 | `test_same_period_multiple_revisions` | v1/v2 两个修订版本，asof 选择正确版本 |
| 字段缺失版本 | `test_missing_field_revision` | v2 的 NetProfit 为 NaN，选中时保持 NaN |
| 未来修订排除 | `test_future_revision_excluded` | v2 在 asof 前不可见，v2 公告后才可见 |
| 未来报告期排除 | `test_future_period_excluded` | 2023FY 在 2024-04-30 前不可见 |
| 同日多报告+revision | `test_same_day_multi_report_revision` | 100 次打乱 x 3 个 asof 日期，结果完全一致 |
| TTM 正确性 | `test_pit_ttm_correctness` | 验证 TTM 公式：current + prev_annual - prev_same |
| PE 自算 | `test_pe_is_price_divided_by_pit_eps` | PE=39/6=6.5, 10/1=10; 负/零 EPS -> NaN |
| 外部 PE 不使用 | `test_external_pe_not_used` | 外部 PE=100 被忽略，PitLayer 返回 PE=10 |
| Snapshot 可追溯 | `test_pit_snapshot_traceability` | Snapshot 包含 report_period/announcement_date/revision_version |

## 十二、仍存在的问题

1. **公告日精度**：当前 `announcement_date` 全部为 `ESTIMATED`（法规截止日近似），非真实公告日期。PIT 精度受此限制。
2. **修订历史完整性**：同花顺数据源可能不提供完整的财务修订历史，部分报告期可能只有首次公告版本。
3. **pe_asof() 保留**：外部 PE 函数标记为 LEGACY 但未删除，保持向后兼容。

## 十三、V1.7 建议

1. 引入真实公告日期数据源（如交易所公告平台），替代 ESTIMATED 近似
2. 增加财务修订历史的完整度校验
3. 考虑删除 pe_asof() 和 pe_step() 等 LEGACY 代码
4. 增加更多 fundamental 模块的 PIT 攻击测试（cashflow.py 的 tail(3) 隐式排序依赖）
5. 增加回测引擎的 PIT 攻击测试（确保回测信号完全基于 PIT 数据）
