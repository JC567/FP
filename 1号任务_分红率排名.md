# 1号任务：A股分红率排名（含排除合并与筛选）

> **任务命名与触发词**
> 本任务编号为 **「1号任务」**。此后任何会话中，用户只要说 **"执行1号任务"**，即指按本文档的完整逻辑重新生成：
> - `data/分红率排名.csv`（沪深主板全量）
> - `data/分红率排名（筛选后）.csv`（排除后的最终结果）
> 两份文件都必须合并「排除」文件夹合集数据的 J~M 列。

---

## 一、任务目标

计算 A 股沪深主板股票的分红率，并做多级筛选，最终输出两份结果文件，且结果文件需附带来自 `data/排除/` 文件夹合集的 J/M/L/K 数据列，并在昨日分红率之后增加「当前PE / 当前PB / 近10年PE百分位」列（已去掉「分红率涨跌幅」列）。

**硬性要求：**
1. 所有筛选逻辑排除**创业板 / 科创板**（以及北交所），仅保留沪深主板。
2. 所有生成的输出文件，都从 `data/排除/` 文件夹下**合集的每一只股票**取出 J~M 列，合并到两份结果文件中。
3. 在昨日分红率之后新增「当前PE」「当前PB」「近10年PE百分位」三列；**不包含**「分红率涨跌幅」列。
4. 近10年PE百分位采用**雪球排名式口径**：百分位 = 历史样本中低于当前PE的天数 / 总样本天数 × 100。
5. 历史数据全部落在**本地 SQLite 数据库**（`D:\stockdata\hist.db`），重复运行优先读本地、不重复远程抓取。

---

## 二、核心字段定义

| 列 | 含义 |
|----|----|
| J | 是否保留（`是` / `否` / 空） |
| K | 买入提醒 pe |
| L | 买入提醒价格 |
| M | 买入 pb |

---

## 三、数据源与接口

- 库：`akshare`、`pandas`
- 分红数据：`ak.stock_fhps_em(date=报告期)`
- 行情数据（最新价/昨收）：
  1. `ak.stock_zh_a_spot`（新浪，优先）
  2. `ak.stock_zh_a_spot_em`（东财，备用）
  3. 均失败则回退本地缓存 `data/spot_cache.csv`
- `data/排除/` 目录：手动的排除文件（.csv，GBK/utf-8 均需兼容）

> 注意：新浪/东财实时行情接口**不稳定、易被限流**（报错 `RemoteDisconnected` / `JSONDecodeError`），接口必须带重试；重试仍失败则回退本地行情缓存。

---

## 四、计算与筛选规则（务必严格照做）

### 4.1 分红数据构建
1. 拉取报告期：`20240930 / 20241231 / 20250331 / 20250630 / 20250930`（覆盖 2025 年度分红）。
2. 剔除非法登记日、空现金分红比例。
3. 判定口径：股权登记日在 **2025-01-01 ~ 2025-12-31**、现金分红比例 **> 0**、方案进度含 **"实施"**。
4. 同一笔分红可能跨报告期重复 → 按 `（代码 + 股权登记日）` 去重，`keep='first'`。
5. `每股分红 = 现金分红比例(每10股派现金) / 10`，按代码求和为 `每股分红合计`。

### 4.2 分红连续性（近5年）
- 取财政年度 `20201231 / 20211231 / 20221231 / 20231231 / 20241231` 五个年报。
- 每年都存在现金分红（现金分红比例 > 0）的股票，取**交集**为"连续5年分红"代码集合，结果只保留这些代码。

### 4.3 板块与 ST 筛选
- **仅保留沪深主板**：`代码` 前 2 位 ∈ `{'60', '00'}`。
- 这样即可同时排除：创业板 `300/301`、科创板 `688/689`、北交所 `8xx/4xx`。
- 排除名称中含 `ST` 的股票。

### 4.4 分红率计算
- `最新分红率 = 每股分红合计 / 最新价 × 100`
- `昨日分红率 = 每股分红合计 / 昨收 × 100`
- 最终筛选：`最新分红率 > 3.0%`，按最新分红率降序，加序号 `排名` 列。
- 注：已**去掉「分红率涨跌幅」列**；新增「当前PE/当前PB」列位于昨日分红率之后。

### 4.4b 近10年PE百分位（当前PB 之后，雪球口径，逐日精度）
- 数据源：`ak.stock_value_em(symbol=代码)`（**东财逐日**估值），覆盖约 **8.6 年逐交易日**，每只约 **2090 个样本**（东财该接口上限约 2093 行/只）。相比原百度月频（约731点）精度大幅提升。
- 取序列**最后一日**为当前 PE-TTM。
- **百分位 = 历史样本中低于当前PE的天数 / 总样本天数 × 100**（排名式，与雪球一致）。
- 同时输出两列：`当前PE`（末值）、`当前PB`（末值）。
- 插入位置：`昨日分红率` 之后，依次为 `当前PE`、`当前PB`、`近10年PE百分位`。
- 逐日PE落库到 `pe_hist`、逐日PB落库到 `pb_hist`（永久缓存），复算时只从本地读取、不再远程抓取。

### 4.4c 是否满足近5年增长（当前PB 之后、近10年PE百分位 之前）
- 数据源：`ak.stock_financial_abstract(symbol=代码)` 的「营业总收入」「净利润」，取最近5个完整会计年度（2021~2025 年报）。
- 判断为 **是** 需同时满足：
  1. 5年内任何一年的营收、净利润均 > 0（不允许为负）；
  2. 上一年(最新)营收与净利润 均 > 5年前(最早) 相应值（整体趋势增长）；
  3. 中间年份「下降年份」≤ 2（下降年份 = 该年营收与净利润均较上一年下降）。
- 否则为 **否**。
- 年度数据落库到 `finance5(code, year, revenue, net_profit)`（永久缓存），复算走本地。

### 4.7 本地历史存储（SQLite）
- 数据库：`D:\stockdata\hist.db`（可用环境变量 `STOCK_DB` 覆盖路径）。
- 表：
  | 表 | 内容 | 缓存策略 |
  |----|----|----|
  | `dividend` | 分红数据（按报告期） | 历史稳定，永久缓存 |
  | `spot` | 最新行情快照 | 当日新鲜度复用（`meta.spot_fetch_date`） |
  | `pe_hist` | 每只股票逐日 PE-TTM 序列 | 历史稳定，永久缓存 |
  | `pb_hist` | 每只股票逐日 PB 序列 | 历史稳定，永久缓存 |
  | `finance5` | 每只股票最近5个年度(营收,净利润) | 历史稳定，永久缓存 |
  | `meta` | 元信息（如行情抓取日期） | — |
- 逻辑：凡是本地已有数据就**优先读本地**；只有本地缺失才远程抓取并写回，保证第二次运行不再重复抓取。

### 4.5 排除数据合并（J~M 列）与最终筛选（第二步脚本）
- 读取 `data/排除/` 下**全部** .csv，提取：`代码`、J（是否保留）、K（买入提醒pe）、L（买入提醒价格）、M（买入pb）。
- 编码依次尝试 `utf-8-sig / gbk / gb18030`。
- 合并去重：按 `代码` 分组，对 J/K/L/M **逐列取第一个非空值（coalesce）**，目的是**不丢数据**——某代码在一个文件为空（可能只是备注），在另一文件有值（如 J=是、L=价格），必须保留有值的那一份（反例：伊利股份 600887）。
- **排除规则**：只要 `data/排除/` 下**任一文件**的 J 列 == `否`，该代码即被排除（求并集）。
- **手动配置为权威**：若存在 `手动配置.csv`（应用「手动配置」页签保存），其 L~O 列**覆盖**其他文件、J 列（是否保留）以它为准（例如手动设为"是"的股票不会被其他文件里的"否"排除）。
- 为了展示一致：凡是命中排除并集的代码，其合集 J 列统一置为 `否`。
- 将合集 `J/M/L/K` 左连接到《分红率排名.csv》生成全量；再从全量中去掉排除并集代码，得到《分红率排名（筛选后）.csv》。

### 4.6 代码归一化（重要，避免前导零丢失）
凡读入/写出的代码都必须统一处理，否则 `000001` 会被当成 `1`、合并错乱：
```python
codes = s.astype(str).str.replace(r'\D', '', regex=True).str.zfill(6)
```

---

## 五、输入文件与目录布局

```
first-project/
├── dividend_rank.py          # 第一步：生成主板全量排名（含PE百分位列）
├── apply_exclusion.py        # 第二步：合并排除数据 J~M + 排除/筛选
├── stock_db.py               # 本地历史数据存储层（SQLite, D:\stockdata\hist.db）
├── check_pe.py               # 复核 PE 百分位的校验工具
├── run_task.py               # 桌面程序（tkinter）
├── 启动1号任务.bat           # 双击启动桌面程序
├── data/
│   ├── 分红率排名.csv            # 输出①（主程序生成，含 J~M 由第二步合并）
│   ├── 分红率排名（筛选后）.csv  # 输出②（合并后筛选结果）
│   ├── 分红率排名（筛选后）.xlsx  # 输出③（带黄/绿高亮的彩色表格）
│   └── 排除/                   # 手动排除文件合集
│       ├── 分红率排名_排除.csv
│       ├── 分红率排名（筛选后）20260813.csv
│       └── 手动配置.csv          # 应用内编辑 L~O 列后自动生成（权威覆盖）
└── D:\stockdata\hist.db       # 本地历史数据库（分红/行情/PE序列）
```

---

## 六、运行步骤（复现）

> 两个脚本必须**按顺序**执行。若重跑，务必先删除旧的输出文件，避免 `_x/_y` 重复列残留。
> 一般直接双击 `启动1号任务.bat`，在应用内点击「运行1号任务」即可，无需手动执行下方命令。

### 桌面程序三个页签
- **运行**：一键执行 第一步+第二步，实时日志；完成后在「数据查看」页签直接查看数据，无需打开本地文件（实际 csv/xlsx 仍生成于 data/）。
- **数据查看**：直接在应用内查看《分红率排名.csv》/《分红率排名（筛选后）.csv》（可切换 全量/筛选后），带黄(PE百分位<30)/绿(满足提醒)高亮。
- **手动配置**：双击编辑 L~O 列（是否保留/买入提醒pe/买入提醒价格/买入pb），可搜索、添加股票、从结果载入；点「保存配置」写入 `data/排除/手动配置.csv`，**下次运行自动按新值计算、判断与展示**。

### 命令行方式（等价）
```powershell
# 1) 删除旧输出（重要，防止列变 _x/_y）
Remove-Item -LiteralPath "data\分红率排名.csv","data\分红率排名（筛选后）.csv" -Force -ErrorAction SilentlyContinue

# 2) 第一步：生成沪深主板全量排名（含近10年PE百分位列）
#    首次运行会抓取全量并写入本地数据库 D:\stockdata\hist.db；之后自动读本地，显著变快
python dividend_rank.py

# 3) 第二步：合并 排除文件夹 的 J~M 列 + 生成筛选后文件
python apply_exclusion.py
```

预期输出（`apply_exclusion.py` 控制台）：
```
排除文件夹合集: 463 只（列 ['代码', '是否保留', '买入提醒pe', '买入提醒价格', '买入pb']）
任一处标记为"否"的排除代码数: 141
《分红率排名.csv》: 413 只
《分红率排名（筛选后）.csv》: 274 只
```

> 注：以上数量基于一次实际运行（行情快照约 2026-08-18，`data/排除/` 已更新为 466 只/176 否）。**若行情/排除文件更新，数量会变化，属正常现象**；判定"复现成功"应以「逻辑一致 + 伊利股份(600887) J=是、买入提醒价格=24.0 且仍在结果中 + 存在近10年PE百分位列」为关键校验点。

---

## 七、关键校验点（验证是否正确）

| 校验项 | 期望 |
|--------|------|
| 结果中无创业板(300/301)/科创板(688/689)/北交所代码 | 代码前缀全部为 60/00 |
| 结果无 `_x` / `_y` 重复列 | `False` |
| 伊利股份 600887 | 保留，J=`是`，买入提醒价格=`24.0`，且 J-M 数据完整 |
| 《分红率排名（筛选后）.csv》无 J=`否` | 全部为 `是` 或空 |
| 存在「当前PE/当前PB/近10年PE百分位」列 | 位于 昨日分红率 之后、是否保留 之前；且**无「分红率涨跌幅」列** |
| PE百分位可复核 | `python check_pe.py 000333` 重算值与 CSV 差值 ≈ 0（排名式口径） |
| 二次运行走本地 | 日志出现「本地缓存/本地今日快照」，耗时明显下降 |

复核工具：`python check_pe.py 代码1 [代码2 ...]`（不带参数则抽查结果文件前5只），逐只从百度估值接口重算百分位并与 CSV 对比。

---

## 八、完整可运行代码

### 8.1 第一步脚本：`dividend_rank.py`

```python
# -*- coding: utf-8 -*-
"""A股分红率排名

计算规则：
- 分红率 = 上一个自然年（2025）内每股分红合计 / 当日收盘价
- 分红筛选：股权登记日在 2025-01-01 ~ 2025-12-31、方案已实施、现金分红比例 > 0
- 分红数据来源：东财分红送配，覆盖股权登记日在 2025 年内的多个报告期
  （20240930/20241231/20250331/20250630/20250930），同一笔按 代码+登记日 去重
  每股分红合计 = 各期现金分红比例(每10股派现金) / 10 之和
- 最新分红率：基于当日收盘价；昨日分红率：基于昨日收盘价（昨收）
- 分红率涨跌幅 = 最新分红率 - 昨日分红率

附加筛选：
1. 近5年每年都有分红：财政年度 2020~2024 年报均存在现金分红（分红连续性）
2. 排除 ST 股票；仅保留沪深主板（排除创业板、科创板、北交所）
3. 筛选条件：最新分红率 > 3.0%
"""
import sys
import io
import time
import os

try:
    sys.stdout.reconfigure(encoding='utf-8')   # 幂等，避免子进程下二次包装产生乱码
except Exception:
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

import akshare as ak
import pandas as pd
import stock_db as db

OUT = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'data')
os.makedirs(OUT, exist_ok=True)

REPORT_DATES = ['20240930', '20241231', '20250331', '20250630', '20250930']
CONTINUITY_YEARS = [f'{y}1231' for y in range(2020, 2025)]  # 近5年(2020~2024)连续分红
RATE_DATE = ('2025-01-01', '2025-12-31')
SZSH_MAINBOARD_PREFIX = {'60', '00'}                  # 沪深主板：沪6(600/601/603/605)、深00(000/001/002/003)
THRESHOLD = 3.0                                         # 最新分红率筛选阈值(%)


def get_code(series):
    return series.astype(str).str.replace(r'\D', '', regex=True).str.zfill(6)


def fetch_fhps(date):
    df = ak.stock_fhps_em(date=date)
    df = df.rename(columns={c: str(c) for c in df.columns})
    return df[['代码', '名称', '现金分红-现金分红比例', '股权登记日', '方案进度']].copy()


def robust_fetch(date):
    for i in range(4):
        try:
            return fetch_fhps(date)
        except Exception as e:
            print(f'[重试] 报告期 {date} 第{i+1}次失败: {e}')
            time.sleep(4)
    raise RuntimeError(f'报告期 {date} 获取失败')


def get_report(conn, report_date):
    """读取某报告期的分红数据；本地无则远程抓取并永久缓存。"""
    df = db.get_dividend(conn, report_date)
    if df is not None:
        print(f'本地缓存报告期 {report_date}（{len(df)} 只）')
        return df
    raw = robust_fetch(report_date)
    df = raw.copy()
    df['现金分红比例'] = pd.to_numeric(df['现金分红-现金分红比例'], errors='coerce')
    df['股权登记日'] = df['股权登记日'].astype(str)
    keep = ['代码', '名称', '现金分红比例', '股权登记日', '方案进度']
    df = df[keep]
    db.save_dividend(conn, report_date, df)
    print(f'已抓取并缓存报告期 {report_date}（{len(df)} 只）')
    return df


def build_dividend():
    """2025 年内每股分红合计（年报+中报）。"""
    conn = db.connect()
    df = pd.concat([get_report(conn, d) for d in REPORT_DATES], ignore_index=True)
    conn.close()
    df['股权登记日'] = pd.to_datetime(df['股权登记日'], errors='coerce')

    # 同一笔分红可能在多个报告期重复出现，按 代码+股权登记日 去重
    df = df.drop_duplicates(subset=['代码', '股权登记日'], keep='first')

    ok = (
        df['股权登记日'].notna()
        & (df['股权登记日'] >= RATE_DATE[0])
        & (df['股权登记日'] <= RATE_DATE[1])
        & (df['现金分红比例'] > 0)
        & df['方案进度'].astype(str).str.contains('实施')
    )
    dm = df[ok].copy()
    dm['每股分红'] = dm['现金分红比例'] / 10.0

    per = (
        dm.groupby('代码')
        .agg(名称=('名称', 'first'), 每股分红合计=('每股分红', 'sum'))
        .reset_index()
    )
    per['代码'] = get_code(per['代码'])
    return per


def continuous_dividend_codes():
    """近5年（2020~2024 年报）每年都有现金分红的股票代码集合。"""
    conn = db.connect()
    yearly = {}
    for d in CONTINUITY_YEARS:
        df = get_report(conn, d)
        codes = set(get_code(df[df['现金分红比例'] > 0]['代码']))
        yearly[d] = codes
        print(f'近5年分红检查 {d}: {len(codes)} 只有现金分红')
    conn.close()
    common = set.intersection(*yearly.values())
    print(f'连续5年分红（{len(common)} 只）')
    return common


def fetch_spot():
    """全市场最新价/昨收，优先用本地今日快照；否则抓新浪/东财并存库。"""
    conn = db.connect()
    cached = db.get_spot(conn)
    if cached is not None and db.get_meta(conn, 'spot_fetch_date') == db.today():
        print('行情: 使用本地今日快照')
        conn.close()
        return cached[['代码', '最新价', '昨收']]

    s = None
    sources = [('新浪', ak.stock_zh_a_spot), ('东财', ak.stock_zh_a_spot_em)]
    for label, fn in sources:
        for i in range(6):
            try:
                s = fn()
                print(f'行情来源: {label}')
                break
            except Exception:
                time.sleep(5)
        if s is not None:
            break

    if s is None:
        if cached is not None:
            print('实时行情获取失败，使用本地最近一次快照')
            conn.close()
            return cached[['代码', '最新价', '昨收']]
        raise RuntimeError('实时行情获取失败且本地无快照')

    s['代码'] = get_code(s['代码'])
    keep = ['代码'] + [c for c in s.columns if str(c) in ('最新价', '昨收', '现价')]
    s = s[keep].copy()
    for c in s.columns[1:]:
        s[c] = pd.to_numeric(s[c], errors='coerce')
    s = s.rename(columns={'现价': '最新价'})
    s = s[s['最新价'] > 0]
    s = s[['代码', '最新价', '昨收']].copy()
    db.save_spot(conn, s.assign(名称=s['代码']), db.today())
    conn.close()
    return s


PE_CACHE = os.path.join(OUT, 'pe_percentile_cache.csv')


def fetch_daily(code):
    """东财逐日 PE(TTM)/市净率（约8.6年，2093个交易日）。返回 df(日期, value=PE, pb=PB)。"""
    for i in range(4):
        try:
            df = ak.stock_value_em(symbol=code)
            d = df.rename(columns={'数据日期': '日期'})[['日期', 'PE(TTM)', '市净率']].copy()
            d = d.rename(columns={'PE(TTM)': 'value', '市净率': 'pb'})
            d['日期'] = pd.to_datetime(d['日期']).astype(str)
            d['value'] = pd.to_numeric(d['value'], errors='coerce')
            d['pb'] = pd.to_numeric(d['pb'], errors='coerce')
            return d[['日期', 'value', 'pb']]
        except Exception:
            time.sleep(2)
    return None


def _current_pb(conn, code, raw):
    if raw is not None:
        pbv = raw['pb'].astype(float).dropna()
        if not pbv.empty:
            return float(pbv.iloc[-1])
    pbh = db.get_pb(conn, code)
    if pbh is not None:
        pbv = pbh['value'].astype(float).dropna()
        if not pbv.empty:
            return float(pbv.iloc[-1])
    return None


def add_pe_percentile(codes):
    """基于东财逐日 PE 序列计算当前 PE-TTM 排名百分位（雪球口径），并返回当前 PE/PB。

    百分位 = 历史样本中低于当前PE的天数 / 总样本天数 × 100。
    逐日序列（PE/PB）落库到 pe_hist / pb_hist，本地已有则直接复用，精度更高。
    返回 (pct列表, 当前PE列表, 当前PB列表)。
    """
    conn = db.connect()
    ready = db.get_pe_ready(conn)
    pct_out, pe_out, pb_out = [], [], []
    newly = []
    total = len(codes)
    for i, code in enumerate(codes, 1):
        raw = None
        h = db.get_pe(conn, code)
        if h is None or code not in ready:
            raw = fetch_daily(code)
            if raw is None or raw['value'].astype(float).dropna().empty:
                pct_out.append(pd.NA); pe_out.append(pd.NA); pb_out.append(pd.NA)
                time.sleep(0.1)
                continue
            db.replace_pe(conn, code, raw[['日期', 'value']].dropna())
            db.replace_pb(conn, code, raw[['日期', 'pb']].dropna().rename(columns={'pb': 'value'}))
            newly.append(code)
            h = raw
        vals = h['value'].astype(float).dropna()
        cur = vals.iloc[-1]
        pct_out.append(round(float((vals < cur).mean() * 100), 2))
        pe_out.append(cur)
        pb_out.append(_current_pb(conn, code, raw))
        time.sleep(0.1)
        if i % 25 == 0 or i == total:
            print(f'[PE进度] {i}/{total}')
    if newly:
        db.mark_pe_ready(conn, newly)
    conn.close()
    return pct_out, pe_out, pb_out


def fetch_growth5(code):
    """抓取最近5个完整会计年度的 (年份, 营业总收入, 净利润)。返回 [(year, rev, np)]。"""
    for i in range(4):
        try:
            df = ak.stock_financial_abstract(symbol=code)
            df = df.rename(columns={c: str(c).strip() for c in df.columns})
            df['指标'] = df['指标'].astype(str).str.strip()
            rev_row = df.loc[df['指标'] == '营业总收入']
            np_row = df.loc[df['指标'] == '净利润']
            periods = [c for c in df.columns if c not in ('选项', '指标') and str(c).endswith('1231')]
            periods = sorted(periods)[-5:]   # 最近5个完整年度
            if rev_row.empty or np_row.empty or len(periods) < 5:
                return None
            rev = pd.to_numeric(rev_row.iloc[0][periods], errors='coerce')
            np_ = pd.to_numeric(np_row.iloc[0][periods], errors='coerce')
            return [(int(c[:4]), float(r), float(n)) for c, r, n in zip(periods, rev, np_) if pd.notna(r) and pd.notna(n)]
        except Exception:
            time.sleep(2)
    return None


def growth_flag(years, rev, np_):
    """判断是否满足近5年增长。

    规则：
      1. 5年内任何一年的营收、净利润均 > 0（不允许为负）。
      2. 上一年(最新) 营收与净利润 均 > 5年前(最早) 相应值（整体趋势增长）。
      3. 中间年份中"下降年份" <= 2（下降年份 = 营收与净利润均较上一年下降）。
    满足 -> 是，否则 -> 否。
    """
    if len(years) < 5:
        return '否'
    r, n = list(rev), list(np_)
    last5 = list(zip(r[-5:], n[-5:]))
    if any(rr <= 0 or pp <= 0 for rr, pp in last5):
        return '否'
    latest, first = last5[-1], last5[0]
    if latest[0] <= first[0] or latest[1] <= first[1]:
        return '否'
    down = 0
    prev = last5[0]
    for y in last5[1:-1]:            # 中间年份
        if y[0] < prev[0] and y[1] < prev[1]:
            down += 1
        prev = y
    return '是' if down <= 2 else '否'


def add_growth_flag(codes):
    """计算"是否满足近5年增长"，并缓存年度财务数据到 finance5。返回 是/否 列表。"""
    conn = db.connect()
    ready = db.get_growth_ready(conn)
    out = []
    newly = []
    total = len(codes)
    for i, code in enumerate(codes, 1):
        rows = db.get_finance5(conn, code)
        if rows is None or code not in ready:
            raw = fetch_growth5(code)
            if raw is None or len(raw) < 5:
                out.append('否')
                time.sleep(0.1)
                continue
            db.replace_finance5(conn, code, raw)
            newly.append(code)
            rows = raw
        years = [r[0] for r in rows]
        rev = [r[1] for r in rows]
        np_ = [r[2] for r in rows]
        out.append(growth_flag(years, rev, np_))
        time.sleep(0.1)
        if i % 25 == 0 or i == total:
            print(f'[增长进度] {i}/{total}')
    if newly:
        db.mark_growth_ready(conn, newly)
    conn.close()
    return out


def main():
    per = build_dividend()
    continuous = continuous_dividend_codes()
    spot = fetch_spot()

    merged = per.merge(spot, on='代码', how='inner')
    # 仅保留沪深主板，排除创业板 300/301、科创板 688/689、北交所
    merged = merged[merged['代码'].str[:2].isin(SZSH_MAINBOARD_PREFIX)]
    # 排除 ST
    merged = merged[~merged['名称'].astype(str).str.contains('ST')]
    # 近5年分红未中断
    merged = merged[merged['代码'].isin(continuous)]

    merged['最新分红率'] = merged['每股分红合计'] / merged['最新价'] * 100.0
    merged['昨日分红率'] = merged['每股分红合计'] / merged['昨收'] * 100.0

    res = merged[merged['最新分红率'] > THRESHOLD].copy()
    res = res.sort_values('最新分红率', ascending=False).reset_index(drop=True)
    res.insert(0, '排名', res.index + 1)

    print('正在计算近10年PE百分位（首次较慢，之后走缓存）...')
    pct, cur_pe, cur_pb = add_pe_percentile(res['代码'].tolist())
    print('正在计算近5年增长（营收/净利润）...')
    growth = add_growth_flag(res['代码'].tolist())
    # 在昨日分红率之后插入 当前PE、当前PB、是否满足近5年增长、近10年PE百分位
    base = res.columns.get_loc('昨日分红率')
    res.insert(base + 1, '当前PE', cur_pe)
    res.insert(base + 2, '当前PB', cur_pb)
    res.insert(base + 3, '是否满足近5年增长', growth)
    res.insert(base + 4, '近10年PE百分位', pct)
    ok_n = int(pd.Series(pct).notna().sum())
    print(f'近10年PE百分位计算完成: {ok_n}/{len(res)} 只')

    print(f'\n最新分红率 > {THRESHOLD}% 的股票共 {len(res)} 只\n')
    cols = ['排名', '代码', '名称', '最新分红率', '昨日分红率', '当前PE', '当前PB',
            '是否满足近5年增长', '近10年PE百分位', '每股分红合计', '最新价', '昨收']
    print(res[cols].head(12).to_string(index=False, float_format=lambda x: f'{x:.4f}'))

    res.to_csv(os.path.join(OUT, '分红率排名.csv'), index=False, encoding='utf-8-sig')
    print(f'\n结果已保存 -> data/分红率排名.csv（共 {len(res)} 只）')


if __name__ == '__main__':
    main()
```

### 8.2 第二步脚本：`apply_exclusion.py`

> 关键：排除文件合集去重要用**逐列第一个非空值（coalesce）**，配合"任一文件 J==否 即排除"的并集规则，这样既保留有值数据、又不错排。

```python
# -*- coding: utf-8 -*-
import os, glob
import pandas as pd

DATA = os.path.dirname(os.path.abspath(__file__)) + os.sep + 'data'
EXCL_DIR = DATA + os.sep + '排除'
RANK = DATA + os.sep + '分红率排名.csv'
OUT_FULL = RANK
OUT_FILT = DATA + os.sep + '分红率排名（筛选后）.csv'

def norm_code(s):
    return s.astype(str).str.replace(r'\D', '', regex=True).str.zfill(6)

def read_any_encoding(path):
    for enc in ('utf-8-sig', 'gbk', 'gb18030'):
        try:
            return pd.read_csv(path, encoding=enc)
        except Exception:
            continue
    raise ValueError(f'无法解析 {path}')

def union_exclude_data():
    files = glob.glob(os.path.join(EXCL_DIR, '*.csv'))
    if not files:
        raise FileNotFoundError(f'排除文件夹为空: {EXCL_DIR}')
    frames = []
    for f in files:
        df = read_any_encoding(f)
        df['代码'] = norm_code(df['代码'])
        jcol = next((c for c in df.columns if '保留' in str(c) or '是否' in str(c)), None)
        out_cols = ['代码']
        if jcol:
            out_cols.append(jcol)
        for c in df.columns:
            if str(c) in ('买入提醒pe', '买入提醒价格', '买入pb'):
                out_cols.append(c)
        frames.append(df[out_cols])
    uni = pd.concat(frames, ignore_index=True)
    def first_nonnull(s):
        v = s.dropna()
        return v.iloc[0] if len(v) else pd.NA
    agg = uni.groupby('代码').agg({c: first_nonnull for c in uni.columns if c != '代码'})
    return agg.reset_index()

def collect_no_codes():
    no = set()
    for f in glob.glob(os.path.join(EXCL_DIR, '*.csv')):
        df = read_any_encoding(f)
        jcol = next((c for c in df.columns if '保留' in str(c) or '是否' in str(c)), None)
        if jcol is None:
            continue
        df['代码'] = norm_code(df['代码'])
        no |= set(df.loc[df[jcol].astype(str).str.strip() == '否', '代码'])
    return no

def main():
    rank = pd.read_csv(RANK, encoding='utf-8-sig')
    rank['代码'] = norm_code(rank['代码'])
    excl = union_exclude_data()
    no_codes = collect_no_codes()
    jcol = next((c for c in excl.columns if '保留' in str(c) or '是否' in str(c)), None)
    if jcol is not None:
        excl.loc[excl['代码'].isin(no_codes), jcol] = '否'
    print(f'排除文件夹合集: {len(excl)} 只（列 {list(excl.columns)}）')
    print(f'任一处标记为"否"的排除代码数: {len(no_codes)}')
    full = rank.merge(excl, on='代码', how='left')
    full.to_csv(OUT_FULL, index=False, encoding='utf-8-sig')
    keep = full[~full['代码'].isin(no_codes)]
    keep.to_csv(OUT_FILT, index=False, encoding='utf-8-sig')
    print(f'《分红率排名.csv》: {len(full)} 只')
    print(f'排除  {len(full)-len(keep)} 只 -> 《分红率排名（筛选后）.csv》: {len(keep)} 只')

if __name__ == '__main__':
    main()
```

---

### 8.3 本地数据存储层：`stock_db.py`

```python
# -*- coding: utf-8 -*-
"""本地历史数据存储（SQLite，位于 D:\\stockdata\\hist.db）。

表结构：
- dividend(report_date, code, name, cash_ratio, reg_date, progress)  分红数据，按报告期缓存（历史稳定，永久缓存）
- spot(code, name, price, pre_close, fetch_date)                     最新行情快照（按日期新鲜度复用）
- pe_hist(code, trade_date, pe_ttm)                                  近10年 PE-TTM 序列（历史稳定，永久缓存）
- meta(key, value)                                                   元信息（如行情抓取日期）

若环境变量 STOCK_DB 指定了路径则优先使用，否则用默认 D 盘路径。
"""
import os
import sqlite3
import datetime

import pandas as pd

DB_PATH = os.environ.get('STOCK_DB', r'D:\stockdata\hist.db')


def connect():
    d = os.path.dirname(DB_PATH)
    if d and not os.path.exists(d):
        os.makedirs(d, exist_ok=True)
    conn = sqlite3.connect(DB_PATH)
    conn.execute('PRAGMA journal_mode=WAL')
    conn.executescript('''
        CREATE TABLE IF NOT EXISTS dividend(
            report_date TEXT, code TEXT, name TEXT, cash_ratio REAL,
            reg_date TEXT, progress TEXT,
            PRIMARY KEY(report_date, code));
        CREATE TABLE IF NOT EXISTS spot(
            code TEXT PRIMARY KEY, name TEXT, price REAL, pre_close REAL,
            fetch_date TEXT);
        CREATE TABLE IF NOT EXISTS pe_hist(
            code TEXT, trade_date TEXT, pe_ttm REAL,
            PRIMARY KEY(code, trade_date));
        CREATE TABLE IF NOT EXISTS pb_hist(
            code TEXT, trade_date TEXT, pb REAL,
            PRIMARY KEY(code, trade_date));
        CREATE TABLE IF NOT EXISTS finance5(
            code TEXT, year INTEGER, revenue REAL, net_profit REAL,
            PRIMARY KEY(code, year));
        CREATE TABLE IF NOT EXISTS valuation_current(
            code TEXT PRIMARY KEY, pe_ttm REAL, pb REAL, fetch_date TEXT);
        CREATE TABLE IF NOT EXISTS meta(key TEXT PRIMARY KEY, value TEXT);
    ''')
    conn.commit()
    return conn


# ---------- 分红数据 ----------

def get_dividend(conn, report_date):
    df = pd.read_sql_query(
        'SELECT code, name, cash_ratio, reg_date, progress FROM dividend '
        'WHERE report_date=?', conn, params=(report_date,))
    if df.empty:
        return None
    return df.rename(columns={'code': '代码', 'name': '名称',
                              'cash_ratio': '现金分红比例',
                              'reg_date': '股权登记日',
                              'progress': '方案进度'})


def save_dividend(conn, report_date, df):
    rows = []
    for r in df.itertuples(index=False):
        cash = None if pd.isna(r.现金分红比例) else float(r.现金分红比例)
        rows.append((report_date, str(r.代码), str(r.名称), cash,
                     str(r.股权登记日), str(r.方案进度)))
    conn.executemany(
        'INSERT OR REPLACE INTO dividend VALUES (?,?,?,?,?,?)', rows)
    conn.commit()


# ---------- 行情快照 ----------

def get_spot(conn):
    df = pd.read_sql_query(
        'SELECT code, name, price, pre_close FROM spot', conn)
    if df.empty:
        return None
    return df.rename(columns={'code': '代码', 'name': '名称',
                              'price': '最新价', 'pre_close': '昨收'})


def save_spot(conn, df, fetch_date):
    rows = [(str(r.代码), str(r.名称), float(r.最新价), float(r.昨收), fetch_date)
            for r in df.itertuples(index=False)]
    conn.executemany('INSERT OR REPLACE INTO spot VALUES (?,?,?,?,?)', rows)
    set_meta(conn, 'spot_fetch_date', fetch_date)
    conn.commit()


# ---------- PE 历史 ----------

def get_pe(conn, code):
    df = pd.read_sql_query(
        'SELECT trade_date AS 日期, pe_ttm AS value FROM pe_hist WHERE code=?',
        conn, params=(code,)).sort_values('日期')
    return df if not df.empty else None


def save_pe(conn, code, df):
    rows = [(code, str(r.日期), float(r.value)) for r in df.itertuples(index=False)]
    conn.executemany('INSERT OR REPLACE INTO pe_hist VALUES (?,?,?)', rows)
    conn.commit()


def replace_pe(conn, code, df):
    """删除该代码原有全部 PE 后整段重写（用于从低密度切换为逐日数据）。"""
    conn.execute('DELETE FROM pe_hist WHERE code=?', (code,))
    save_pe(conn, code, df)


# ---------- PB 历史 ----------

def get_pb(conn, code):
    df = pd.read_sql_query(
        'SELECT trade_date AS 日期, pb AS value FROM pb_hist WHERE code=?',
        conn, params=(code,)).sort_values('日期')
    return df if not df.empty else None


def replace_pb(conn, code, df):
    conn.execute('DELETE FROM pb_hist WHERE code=?', (code,))
    rows = [(code, str(r.日期), float(r.value)) for r in df.itertuples(index=False)]
    conn.executemany('INSERT OR REPLACE INTO pb_hist VALUES (?,?,?)', rows)
    conn.commit()


# ---------- 逐日就绪标记 ----------

def get_pe_ready(conn):
    v = get_meta(conn, 'pe_daily_ready')
    return set(v.split(',')) if v else set()


def mark_pe_ready(conn, codes):
    ready = get_pe_ready(conn) | set(codes)
    set_meta(conn, 'pe_daily_ready', ','.join(sorted(ready)))


# ---------- 近5年营收/净利润 ----------

def get_finance5(conn, code):
    df = pd.read_sql_query(
        'SELECT year, revenue, net_profit FROM finance5 WHERE code=? ORDER BY year',
        conn, params=(code,))
    return df if not df.empty else None


def replace_finance5(conn, code, rows):
    """rows: [(year, revenue, net_profit)]，删除旧数据后整段重写。"""
    conn.execute('DELETE FROM finance5 WHERE code=?', (code,))
    conn.executemany('INSERT OR REPLACE INTO finance5 VALUES (?,?,?,?)',
                     [(code, int(y), float(r), float(n)) for y, r, n in rows])
    conn.commit()


def get_growth_ready(conn):
    v = get_meta(conn, 'growth_ready')
    return set(v.split(',')) if v else set()


def mark_growth_ready(conn, codes):
    ready = get_growth_ready(conn) | set(codes)
    set_meta(conn, 'growth_ready', ','.join(sorted(ready)))


# ---------- 当前估值（PE/PB） ----------

def get_valuation(conn, code):
    row = conn.execute(
        'SELECT pe_ttm, pb FROM valuation_current WHERE code=?', (code,)).fetchone()
    return (row[0], row[1]) if row else None


def save_valuation(conn, code, pe_ttm, pb):
    conn.execute('INSERT OR REPLACE INTO valuation_current VALUES (?,?,?,?)',
                 (code, pe_ttm, pb, today()))
    conn.commit()


# ---------- 元信息 ----------

def get_meta(conn, key):
    row = conn.execute('SELECT value FROM meta WHERE key=?', (key,)).fetchone()
    return row[0] if row else None


def set_meta(conn, key, value):
    conn.execute('INSERT OR REPLACE INTO meta VALUES (?,?)', (key, str(value)))
    conn.commit()


def today():
    return datetime.date.today().isoformat()
```

### 8.4 复核工具：`check_pe.py`

```python
# -*- coding: utf-8 -*-
"""复核某只股票「近10年PE百分位」，用于检查 CSV 中的值是否正确。

用法:
    python check_pe.py 600887
    python check_pe.py 600887 000858
不带参数则随机抽查结果文件前5只。
"""
import sys, os, io, random
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
import akshare as ak
import pandas as pd

DATA = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'data')
RANK = os.path.join(DATA, '分红率排名（筛选后）.csv')


def norm(s):
    return s.astype(str).str.replace(r'\D', '', regex=True).str.zfill(6)


def compute(code):
    """从东财逐日接口重新计算 近10年PE-TTM 排名百分位（雪球口径）。"""
    df = ak.stock_value_em(symbol=code)
    v = pd.to_numeric(df['PE(TTM)'], errors='coerce').dropna()
    cur = v.iloc[-1]
    pct = float((v < cur).mean() * 100)
    return pct, cur, int(len(v))


def main():
    codes = [c for c in sys.argv[1:]]
    df = pd.read_csv(RANK, encoding='utf-8-sig')
    df['代码'] = norm(df['代码'])
    if not codes:
        codes = df['代码'].head(5).tolist()
    for code in codes:
        row = df[df['代码'] == code]
        if row.empty:
            print(f'{code}: 不在结果文件中')
            continue
        csv_val = row['近10年PE百分位'].iloc[0] if '近10年PE百分位' in df.columns else None
        try:
            pct, cur, n = compute(code)
            diff = ''
            if pd.notna(csv_val):
                diff = f'  差值={pct - float(csv_val):+.2f}'
            print(f'{code}: 当前PE-TTM={cur:.2f} | 重新计算百分位={pct:.2f}% '
                  f'(样本{n}个) | CSV值={csv_val}{diff}')
        except Exception as e:
            print(f'{code}: 复核失败 {e}')


if __name__ == '__main__':
    main()
```

## 九、易踩的坑（复现时务必注意）

1. **行情接口限流**：新浪/东财实时行情经常失败，必须重试 + 本地缓存兜底；否则运行直接中断。
2. **前导零丢失**：`000001` 等代码不做 `zfill(6)` 会合并错乱，读入/输出前都要归一化。
3. **`_x` / `_y` 列**：重复运行并合并在未删除旧文件时，列名会变成 `xxx_x / xxx_y`；重跑前先删除两份输出文件。
4. **排除合并丢数据**：不能 `drop_duplicates(keep='first')`，必须**逐列 coalesce（取第一个非空值）**，否则伊利的"是、24.0"会被空值覆盖。
5. **排除判定范围**：是否排除要看**所有**排除文件的并集，任一文件标记"否"即排除，不能只看第一个文件。
6. **中文**：Windows 终端直接传中文 `-c` 参数会乱码（导致 KeyError），建议把逻辑写进 `.py` 脚本文件执行，或用重定向到 UTF-8 文件再查看。

---

## 十、红利价值分位研究（`valresearch` 模块）

> 在 1号任务（分红率排名）基础上新增的独立估值研究功能，已集成进 `run_task.py` 作为**第 4 个页签**。
> 以「红利价值历史分位数估值模型」为核心：**PE / 股息率 / 分红率** 三指标的历史分位数 + Gordon 增长模型 + 基本面质量 + 价值陷阱 + 信号系统 + 合理价格区间 + 仓位建议，输出**九段式报告**与 **JSON**，并提供**单股历史重估回测**。

### 10.1 架构（按 Phase 分模块）

```
valresearch/
├── models.py                 # dataclass 数据结构（AnalysisReport / PercentileStats / ...）
├── settings.yaml             # 全部关键参数（mode 覆盖阈值，不写死）
├── config.py                 # 配置加载 + 三模式深合并（conservative/balanced/aggressive）
├── main.py                   # 编排器 analyze(symbol, date, mode) -> AnalysisReport
├── main_valresearch.py       # CLI 入口
├── data/
│   ├── providers.py          # 5 个数据 Provider（价格/PE/财报/分红/国债/行业），本地缓存
│   ├── pit.py                # 时点快照（PIT）语义：eps_ttm_asof / dps_ttm_asof / price_asof / pe_asof
│   └── quality.py            # 数据质量检查 + hard_block（不合格拒出结论）
├── valuation/
│   ├── engine.py             # 估值序列构建（PE/股息率/分红率 逐日 PIT）
│   ├── percentile.py         # 严格 count 口径分位 + 异常值处理 + P20/P30/P70/P80
│   ├── gordon.py             # GGM 合理PE + Bear/Base/Bull 情景矩阵（g>=Ke 防护）
│   └── price_range.py        # 合理价/股息率反推价/历史分位价/买入区间分档
├── fundamental/              # 盈利稳定性/现金流/分红持续性/负债/行业/质量评分
├── risk/value_trap.py        # 价值陷阱评分（>60 禁 STRONG_BUY）
├── signal/                   # 三条件规则 + 综合评分 + 陷阱仲裁 + 仓位表
├── backtest/                 # 单股历史重估回测 + 绩效指标 + 基准对比
├── report/                   # 九段式报告 + JSON 输出
└── gui/tab.py                # 桌面页签（已注册进 run_task.py）
```

### 10.2 数据源（与 1号任务不同）

| 数据 | 接口 | 说明 |
|------|------|------|
| 日线 | 新浪 `stock_zh_a_daily` | 原价 close（估值用）+ 后复权 adj_close（收益/回测用） |
| PE-TTM | 百度 `stock_zh_valuation_baidu` | as-reported，打 `PE_TTM_PIT_APPROXIMATION` 近似标记 |
| 财报 | 同花顺 `stock_financial_abstract_ths` | 公告日按法规截止日近似（12-31→次年4-30；9-30→10-31；6-30→8-31；3-31→4-30） |
| 分红 | 巨潮 `stock_dividend_cninfo` | 实施方案公告日=implement_date（已实施即公开，无前视） |
| 10Y国债 | `bond_zh_us_rate` | 列「中国国债收益率10年」（`macro_china_10y_treasury` 不可用） |
| 行业 | 东财 `stock_individual_info_em` → 名称关键词推断 | 东财限流时回退推断，明示主观 |
| 基准 | 新浪 `stock_zh_index_daily` | 沪深300 `sh000300`（东财指数接口被墙） |

- 并发：`_MINI_LOCK` 串行化 ths/cninfo（py_mini_racer V8 多线程段错误）；`_AK_LOCK` 全局串行抓取。
- 全部落本地 SQLite `vr_*` 表（vr_prices/vr_valuation/vr_financials/vr_dividends/vr_macro/vr_industry/vr_analysis/vr_backtest），二次运行走缓存。

### 10.3 关键口径（严格 PIT，禁止未来函数）

1. **分红率（每股口径）**：`payout = DPS_TTM / EPS_TTM`，数学等价 `现金分红总额/归母净利润`，免总股本。
   - `DPS_TTM` = 近12个月已实施（implement_date）现金分红滚动和；`EPS_TTM` = 最新已公告报告期 + 上年年报 − 上年同期外推。
2. **分位（严格 count）**：`Pct = count(历史样本 < 当前值) / 有效样本 × 100`（非 pandas 线性插值）。
   - 负 PE 剔除（不参与分位）；分红率 `<0 或 >150%` 打 abnormal 且剔出分位样本，winsorize 前后各 1% 但保留原始值。
   - 提供 10 年主窗口 + 5 年辅窗口。
3. **Gordon**：`FairPE = Payout / (Ke − g)`，`Ke = Rf + ERP`，`g = min(历史增速, 可持续, 行业, 名义GDP)`。
   - `g ≥ Ke` 或 `Ke − g ≤ 2%` → 输出 `GGM_INVALID`，绝不输出虚假合理 PE；情景矩阵 Bear/Base/Bull 用 Ke±1%、g∓1%。
4. **股息率-国债利差**：`spread = 股息率(%) − 10Y国债(%)`，阈值 2%，达标计入信号。
5. **质量评分**（0-100）：盈利稳定性 25% + 现金流 25% + 分红持续性 20% + 负债 15% + 行业 15%。缺失子模块给中间分并打 `DATA_INSUFFICIENT`，不伪造。
6. **价值陷阱**（0-100）：盈利连续下滑/OCF 恶化/分红率长期>100%/高负债/行业衰退/政策风险，逐项加分；`>60` 禁 STRONG_BUY，惩罚系数折减综合分。
7. **信号**：三条件 `A=PE分位<阈值`、`B=股息率分位>阈值`、`C=分红率分位<阈值`；规则信号 + 综合评分信号 + 陷阱仲裁 → 最终信号（STRONG_BUY/BUY/ACCUMULATE/HOLD/WAIT/REDUCE/SELL）。综合评分对 7 个维度全权重加权（PE/股息率/分红率/利差/Gordon/质量/行业）；**任一维度缺失（如 Gordon 因 Ke−g 过小失效）取中性分 50，分母恒为全部权重**，避免不同模式间分数不可比；中性分处理会在报告「依据」中注明（如 `缺失维度按中性分(50)计: gordon`）。分项映射（消除封顶饱和失真）：PE=100−分位、股息率=分位、分红率=100−分位、利差=`50×(利差/阈值)`（阈值处50、2×阈值100）、Gordon=`100−50×(当前PE/合理PE)`（合理价处50、折价加分、溢价减分、对称无封顶）。三条件在回测时序中带**滞回带宽(5%)**，避免边界噪声导致信号来回翻转；单次分析无历史状态，使用严格阈值。样本量<1年交易日时在报告中提示"分位可能系统性偏低"。
8. **价格区间**：GGM 情景合理价、Price@4/5/6/7% 股息率反推价、历史分位价（当前 EPS × 历史 PE_Pxx）、深度/标准买入区、当前所处区间。
9. **仓位**：按最终信号查表（初始/建议/上限），价值陷阱高分自动压降。

### 10.4 三种模式（`settings.yaml` 的 `modes` 覆盖）

| 模式 | PE 分位阈值 | 股息率分位阈值 | Ke_base 偏移 |
|------|------------|---------------|-------------|
| conservative | 20 | 80 | +1% |
| balanced | 30 | 70 | 0 |
| aggressive | 40 | 60 | −1% |

### 10.5 运行方式

**CLI（单股九段式报告 + JSON）：**
```powershell
python main_valresearch.py 600036 --name 招商银行 --date 2025-07-01 --mode balanced --json data\vr_reports\600036.json
# date 留空=今天；json 可选
```

**GUI：** `python run_task.py` → 第 4 个页签「红利价值分位研究」→ 输入代码/名称/日期/模式 → 「单股分析」/「生成JSON」/「单股回测」。

- **模式用中文名**：`稳健型(保守) / 均衡型(默认) / 进取型(激进)`，三种模式的区别（PE分位/股息率分位/Gordon折现率阈值）见页签内「模式说明」弹框。
- **帮助说明**：页签右上「? 帮助说明」弹框，分四页解释：模式区别 / 单股分析规则 / 单股回测规则 / 九段式报告各段含义。
- **运行进度**：单股分析/回测运行时，页签内进度条 + 状态文字实时显示当前阶段（取数→质量→分位→Gordon→信号→完成）。
- **输出为中文**：报告与回测结果中的信号(强烈买入/买入/逢低吸纳/持有/观望/减仓/卖出)、规则、模式、质量标记、指标等英文一律翻译为中文；JSON 数据仍保留英文键（机器可读）。

**回测**：`re_evaluate` 对每周再平衡日做 PIT 重算完整信号；`run_backtest` 按信号进出场 vs 买入持有 vs 基准指数，输出 CAGR/年化波动/Sharpe/最大回撤/Calmar/胜率。默认回测区间 2019-01-01 ~ 2025-12-31。

### 10.6 已知局限（报告中会如实标注）

- 百度历史 PE 为 as-reported（轻微前视，已打标记）；财报公告日按法规截止日近似。
- 同花顺仅给每股经营现金流、缺总股本 → 现金流总量 `DATA_INSUFFICIENT`；总资产/总负债缺失 → 负债模块部分中间分。
- 行业识别东财限流时用名称关键词推断（主观，可配置覆盖）。
- 中证红利基准仅 2019-01 起；红利低波指数代码需按需更换符号。

### 10.7 测试

`tests/` 下按 Phase 分组：`test_phase2.py`（PIT）、`test_phase34.py`（分位）、`test_phase5.py`（Gordon）、`test_phase6789.py`（质量/陷阱/信号/价格）、`test_phase10.py`（回测）、`test_phase11.py`（报告/JSON）。运行：

```powershell
python tests\test_phase6789.py
```

---

*文档编号：1号任务 · 最近一次实测快照：行情缓存约 2026-08-07，排除文件 2026-08-13*